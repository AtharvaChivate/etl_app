import DeletableEdge from './DeletableEdge';
import SimpleDeleteEdge from './SimpleDeleteEdge';

export const edgeTypes = {
  deletable: DeletableEdge,
  simple: SimpleDeleteEdge,
};

export default edgeTypes;
